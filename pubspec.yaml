name: fooddelivery
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  # Use the latest compatible version of pin_code_fields. Run `flutter pub get` or
  # `flutter pub upgrade pin_code_fields` to fetch the newest release.
  pin_code_fields: any
  url_launcher: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
